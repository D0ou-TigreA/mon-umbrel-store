version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: doud-ems-anythingllm_web_1
      APP_PORT: 3001
      PROXY_AUTH_ADD: "false"

  web:
    image: mintplexlabs/anythingllm:latest
    user: root
    restart: on-failure
    cap_add:
      - SYS_ADMIN
    environment:
      STORAGE_DIR: /app/server/storage
      SERVER_PORT: 3001
      JWT_SECRET: "umbrel-ems-874012757431998-660342483393159-198813911385317-292550632631858"
      DISABLE_TELEMETRY: "true"
    volumes:
      - ${APP_DATA_DIR}/data/storage:/app/server/storage
